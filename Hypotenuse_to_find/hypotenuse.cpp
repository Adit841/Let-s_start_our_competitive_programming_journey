#include <iostream>
using namespace std;
#include <math.h>

int main(){
    int perpendicular = 4;
    int base = 3;
    int sum_of_power = pow(3,2) + pow(4,2);
    int hypotenuse = sqrt(sum_of_power);

    cout << hypotenuse;

    return 0;
}