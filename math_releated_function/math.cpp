#include <iostream>
#include <cmath>
using namespace std;

int main(){

    int a = 20;
    int b = 340;

    int z = max(a,b);
    int y = min(a,b);
    int x = pow(2,4);
    int w = sqrt(9);
    int ab = abs(-3);
    int ro = round(3.114);
    

    cout << z <<'\n' << y;
    return 0;
}